package at.acpi.heartfall.neoforge;

//? if neoforge {

/*import at.acpi.heartfall.Heartfall;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.fml.common.EventBusSubscriber;
import net.neoforged.neoforge.event.entity.living.LivingDeathEvent;

@EventBusSubscriber(modid = Heartfall.MOD_ID)
public class NeoForgeLivingEntityDeathListener {
    @SubscribeEvent
    public static void onEntityDeath(LivingDeathEvent event) {
        Heartfall.process(event.getEntity(), event.getSource());
    }
}
*///? }
